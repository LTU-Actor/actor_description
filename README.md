# The `actor_description` package

Provides URDF files for LTU actor robots

## Usage

``` bash
# Launch the actor_description with the vehicle set to the .urdf file name
launch actor_description actor_description.launch vehicle:=actor-1
```
